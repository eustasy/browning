# eustasy/.Normal 4.0beta3
name: Normal (Markdown)

on:
  push:
    branches: [main]
    paths: ["**.md"]
  pull_request:
    branches: [main]
    paths: ["**.md"]

permissions:
  contents: read

jobs:
  md:
    name: Lint + format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: qltysh/qlty-action/install@v2

      - name: Lint + format check (markdownlint)
        run: qlty check --all --filter=markdownlint
